// function to generate markdown for README
function generateMarkdown(userResponses) {

 return `## Objective

The objective of this project is to ${userResponses.title}`

  


}

module.exports = generateMarkdown;
